[project]
name = "recipes-cms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "social-auth-app-django>=5.4.3",
    "wagtail>=7.1.3",
]

[dependency-groups]
dev = [
    "invoke>=2.2.1",
    "ruff>=0.15.1",
]
